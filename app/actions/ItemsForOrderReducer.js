// import getAPIUrl from 'rootSource/apiConfig';
const header = {}

/*export function getCompanyRoleType() {
    const url = getAPIUrl('companyRoleTypeServices');
    return (dispatch) => {
        NetworkService.callService({
            method: "GET",
            url: url,
            headers: header,
            success: function success(response) {
                const type = response.success ? COMPANYTYPE : COMPANYTYPE;
                dispatch({
                    type: type,
                    payload: (response.data || [])
                })
            }
        })
    }
};*/
